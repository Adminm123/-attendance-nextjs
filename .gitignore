# ไม่ commit ไฟล์เหล่านี้ขึ้น git
node_modules/
.next/
.env.local          # ← สำคัญ! เก็บ API keys ห้าม commit เด็ดขาด
.env*.local

# OS files
.DS_Store
Thumbs.db

.vercel
.env*

# Credentials & secrets — ห้าม commit เด็ดขาด
sa_value.txt
*.json.key
service-account*.json

# Build artifacts
tsconfig.tsbuildinfo

# Debug / temp
response.json
